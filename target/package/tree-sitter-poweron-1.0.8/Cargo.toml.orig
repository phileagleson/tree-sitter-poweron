[package]
name = "tree-sitter-poweron"
description = "POWERON grammar for the tree-sitter parsing library"
version = "1.0.8"
keywords = ["incremental", "parsing", "POWERON"]
categories = ["parsing", "text-editors"]
repository = "https://github.com/tree-sitter/tree-sitter-POWERON"
edition = "2018"
license = "MIT"

build = "bindings/rust/build.rs"
include = [
  "bindings/rust/*",
  "grammar.js",
  "queries/*",
  "src/*",
]

[lib]
path = "bindings/rust/lib.rs"

[dependencies]
tree-sitter = "~0.20"

[build-dependencies]
cc = "1.0"
